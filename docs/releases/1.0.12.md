# Release Notes 1.0.12

## Bug Fixes

- Citation insertion now leaves the cursor in the right place
- For frontmatter, add `??` prefix to avoid accidentally overwriting existing values when updating an existing custom field. Check out the [source note](https://zotflow.peterduan.dev/source-notes#frontmatter-always-editable) for details on how frontmatter merging works.
